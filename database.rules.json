{
  "rules": {    
    "users": {
      ".read": "root.child('users/'+auth.uid).child('role').val() === 'ADMIN'",
      ".write": "root.child('users/'+auth.uid).child('role').val() === 'ADMIN'"
    }
  }
}
